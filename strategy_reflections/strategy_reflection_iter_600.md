# Top Quark Reconstruction - Iteration 600 Report

The modest gain to 0.616 ± 0.015 (≈1 σ below the 0.64 target) shows that moving from a Gaussian‑copula to a Student‑t “copula‑like” likelihood successfully rescued many outlier tops that were previously penalised by the single‑linear‑correlation assumption, and the pT‑dependent veto tuning helped retain low‑pT candidates where the mass resolution is poor; however, the weighted geometric mean and the final sigmoid squashing appear to have over‑regularised the score, dampening the discriminating power of the strongest variables and limiting the overall efficiency lift. The next step is to re‑optimize the exponent weights in the geometric mean (or replace it with a learnable monotonic combination), calibrate the sigmoid’s slope and offset to avoid excessive compression, and consider more flexible multivariate models—e.g. a mixture of Student‑t components or a normalising‑flow density estimator—that can capture both heavy‑tailed and pT‑dependent correlations without imposing a single global shape; a targeted cross‑validation on separate low‑ and high‑pT regions could also reveal whether region‑specific score mappings deliver the remaining efficiency boost needed to breach the 0.64 barrier while preserving background rejection.